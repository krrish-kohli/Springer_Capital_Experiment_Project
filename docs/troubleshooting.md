## Troubleshooting (advanced)\n+\n+This page documents **troubleshooting-only** shortcuts. The intended architecture (Ahmad requirement) is:\n+\n+`event_sim → NiFi → ClickHouse bronze → dbt → silver/gold`\n+\n+### 1) Bypass NiFi (troubleshooting only)\n+\n+If you are debugging ClickHouse connectivity (or NiFi is temporarily misconfigured), you can bypass NiFi and have `event_sim` write directly to ClickHouse.\n+\n+In `docker-compose.yml`, set:\n+\n+- `EVENT_TARGET=clickhouse`\n+\n+Then restart:\n+\n+```bash\n+docker compose up -d\n+```\n+\n+This mode is **not** the intended realtime architecture. Use it only to isolate connectivity issues.\n+\n+### 2) Confirm NiFi is ingesting\n+\n+- In NiFi UI, your processors should be running and not backpressured.\n+- In ClickHouse, `bronze.events` row count should increase over time.\n+\n+### 3) Confirm dbt is refreshing silver/gold\n+\n+- Airflow DAG `medallion_dbt_every_5m` should be succeeding.\n+- Tables `silver.fct_events` and `gold.*` should have rows.\n+\n*** End Patch"}]}endphp? Wait ApplyPatch requires freeform not JSON. Use correct.
