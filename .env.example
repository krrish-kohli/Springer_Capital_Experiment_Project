# Copy to .env and fill in secrets (never commit .env).
# Baserow SaaS API — used by Airflow DAG baserow_feedback_to_gold

BASEROW_API_TOKEN=
BASEROW_BASE_URL=https://api.baserow.io
BASEROW_DATABASE_ID=435680
BASEROW_TABLE_ID=968203
